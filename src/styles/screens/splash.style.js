import {StyleSheet} from 'react-native';

export const splashStyles = StyleSheet.create({
  body: {
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
  },
  image: {
    width: 200,
    height: 200,
    resizeMode: 'contain',
    marginBottom: 50,
    borderRadius: 200,
  },
});
